{
  "scripts": {
    "build": "eleventy",
    "deploy": "rm -rf _site dist && npm run build && gh-pages --dist _site --no-history",
    "start": "eleventy --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "1.0.2",
    "gh-pages": "4.0.0",
    "prettier": "2.7.1",
    "puppeteer": "19.2.2",
    "sass": "1.56.0",
    "sharp": "0.31.2"
  }
}

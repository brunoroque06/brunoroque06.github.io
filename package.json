{
  "scripts": {
    "build": "npm run build-cv && npm run style && eleventy",
    "build-cv": "node build-cv.js",
    "deploy": "sed -i '' 's/localhost:8080/brunoroque06.github.io/' _data/origin.js && npm run build && gh-pages --dist _site --no-history && git checkout _data/origin.js",
    "serve": "eleventy --serve",
    "style": "sass --style=compressed --no-source-map _includes/style.scss _includes/style.css"
  },
  "devDependencies": {
    "@11ty/eleventy": "^1.0.0",
    "gh-pages": "3.2.3",
    "prettier": "^2.5.1",
    "puppeteer": "^13.0.1",
    "sass": "^1.48.0"
  }
}

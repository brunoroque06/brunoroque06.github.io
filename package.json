{
  "scripts": {
    "build": "eleventy",
    "deploy": "rm -rf _site dist && npm run build && gh-pages --dist _site --no-history",
    "start": "eleventy --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "1.0.1",
    "gh-pages": "4.0.0",
    "prettier": "2.7.1",
    "puppeteer": "15.4.0",
    "sass": "1.53.0",
    "sharp": "0.30.7"
  }
}

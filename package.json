{
  "private": true,
  "scripts": {
    "format": "git diff --cached --name-only --diff-filter=d | grep -E '^.*\\.(html|json|md|scss|yml)' | xargs prettier --write"
  },
  "devDependencies": {
    "husky": "3.0.9",
    "prettier": "1.18.2"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm run format"
    }
  }
}

{
  "scripts": {
    "build": "eleventy",
    "deploy": "npm run build && gh-pages --dist _site --no-history",
    "serve": "eleventy --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "1.0.0",
    "gh-pages": "3.2.3",
    "prettier": "2.5.1",
    "puppeteer": "13.1.2",
    "sass": "1.49.0"
  }
}

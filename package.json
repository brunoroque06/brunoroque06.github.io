{
  "scripts": {
    "build": "eleventy",
    "deploy": "npm run build && gh-pages --dist _site --no-history",
    "serve": "eleventy --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "1.0.1",
    "gh-pages": "4.0.0",
    "prettier": "2.6.2",
    "puppeteer": "14.0.0",
    "sass": "1.51.0"
  }
}
